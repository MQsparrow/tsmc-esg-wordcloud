from __future__ import annotations

import streamlit as st


st.set_page_config(page_title="TSMC ESG Dashboard", layout="wide")

st.title("TSMC ESG Dashboard")
st.info(
    "This legacy entry point is kept for compatibility. "
    "Run the current dashboard with `streamlit run app.py` from the project root."
)
